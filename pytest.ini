[pytest]
DJANGO_SETTINGS_MODULE = tests.test_settings
addopts = -m "not postgis"
markers =
    postgis: integration tests that need PostGIS; creates/drops simple_locations_test (OS user @ localhost:5432 by default)